FreeRTOS parts in this example were directly copied to project folders -- do not do it in your projects.
Place FreeRTOS in some directory under lib/ and use Makefile or .mk include to build it as library instead.
